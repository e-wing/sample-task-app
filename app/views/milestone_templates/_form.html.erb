<%= form_for(@milestone_template) do |f| %>
  <% if @milestone_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@milestone_template.errors.count, "error") %> prohibited this milestone_template from being saved:</h2>

      <ul>
      <% @milestone_template.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
